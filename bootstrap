#!/bin/sh
set -eu

repo_root="$(CDPATH= cd -- "$(dirname "$0")" && pwd)"

if ! command -v dfl >/dev/null 2>&1; then
  sh "$repo_root/install-dfl.sh"
fi

PATH="$HOME/.local/bin:$PATH" exec dfl setup --repo "$repo_root"
